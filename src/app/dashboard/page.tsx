import { Metadata } from 'next'
import LoginForm from '@/components/login-form'

export const metadata: Metadata = {
  title: 'Dashboard | Livy',
  description: 'Create Livys',
}

export default function Page() {
  return (
    <main className="">
      dashboard
    </main>
  )
}